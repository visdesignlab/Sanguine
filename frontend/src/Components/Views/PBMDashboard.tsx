import { Title } from '@mantine/core';

export function PBMDashboard() {
    return (
        <div style={{ paddingTop: 32, paddingLeft: 24 }}>
            <Title order={1}>Patient Blood Management Dashboard</Title>
        </div>
    );
}
